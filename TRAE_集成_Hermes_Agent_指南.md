# TRAE 集成 Hermes Agent 指南

> 让 TRAE 拥有 Hermes 的永久记忆与自我进化能力
> 更新日期：2026-05-05

---

## 目录

- [一、核心逻辑：Hermes 是什么，不是什么](#一核心逻辑hermes-是什么不是什么)
- [二、集成方案对比](#二集成方案对比)
- [三、方案一：Hermes 做「前置大脑」（推荐）](#三方案一hermes-做前置大脑推荐)
- [四、方案二：API 中转（进阶玩家）](#四方案二api-中转进阶玩家)
- [五、集成后的效果对比](#五集成后的效果对比)
- [六、最佳实践与使用技巧](#六最佳实践与使用技巧)
- [七、常见问题](#七常见问题)

---

## 一、核心逻辑：Hermes 是什么，不是什么

### ⚠️ 先搞清楚关键前提

Trae **自定义模型设置**目前只支持**平台内置的服务商**，不能直接填写本地 localhost 地址。所以 Hermes 不能以"换模型"的方式直接嵌入 Trae。

### Hermes 的正确定位

| 它 **不是** ❌ | 它 **是** ✅ |
|---------------|-------------|
| 一个大模型 | **记忆管家** + **任务规划器** + **自动进化助手** |
| 替代 Trae | **前置大脑**，与 Trae 协同工作 |
| 收费模型 | 100% 开源免费（MIT 协议） |
| 需要换工具 | Trae 该怎么用还怎么用 |

### 一句话理解

> Hermes 是给你的 AI 工具**加一个不会失忆的大脑**，而不是换掉它的大脑。

---

## 二、集成方案对比

| 方案 | 难度 | 推荐度 | 适用人群 |
|:----:|:----:|::----:|:---------|
| **方案一：前置大脑** | ⭐ 极简 | ⭐⭐⭐⭐⭐ **强烈推荐** | 所有人 |
| **方案二：API 中转** | ⭐⭐⭐ 中等 | ⭐⭐⭐ | 进阶玩家 |

核心原则：**不换 Trae、不换模型、不产生额外费用**。

---

## 三、方案一：Hermes 做「前置大脑」（推荐）

### 架构示意

```
你的需求
    ↓
┌─────────────────────┐
│   Hermes Agent      │  ← 负责：记忆、规划、拆解、总结
│   （前置大脑）      │
└─────────┬───────────┘
          ↓ 产出：梳理好的代码/逻辑/方案
┌─────────────────────┐
│   TRAE SOLO         │  ← 负责：编辑、调试、运行、部署
│   （执行环境）      │
└─────────────────────┘
    ↓
   最终交付
```

### 操作步骤

#### 第 1 步：部署 Hermes Agent

```bash
# 一键安装
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash

# 验证安装
hermes --version

# 配置 LLM（选择你原本在 Trae 用的模型提供商）
hermes setup
```

> 💡 部署时绑定你**原本在 Trae 使用的模型**（如 DeepSeek、GLM 等），不需要换模型。

#### 第 2 步：日常使用流程

```
┌──────────────────────────────────────────────────────┐
│ 你的工作流（推荐）                                     │
│                                                        │
│  ① 打开 Hermes 终端 → 用自然语言描述需求               │
│     ↓                                                  │
│  ② Hermes 利用记忆+技能 → 梳理逻辑、生成代码框架       │
│     ↓                                                  │
│  ③ 将 Hermes 输出的代码复制到 Trae                     │
│     ↓                                                  │
│  ④ Trae 中编辑、调试、运行、部署                        │
│     ↓                                                  │
│  ⑤ Hermes 自动记住这次经验 → 下次更聪明                 │
└──────────────────────────────────────────────────────┘
```

#### 第 3 步：实际使用示例

**场景**：做一个新功能模块

在 Hermes 终端中输入：
```
帮我开发一个用户权限管理模块：
- 包含角色 CRUD、权限分配、菜单配置
- 参考我上个月做的"订单管理"项目的代码风格
- 输出完整的代码和目录结构
```

Hermes 会：
1. 🧠 回忆"订单管理"项目的代码风格（跨会话记忆）
2. 📋 自动拆解任务：数据库设计 → API 定义 → 前端页面
3. 🎯 输出结构化的代码框架
4. 💾 将这次经验沉淀为 Skill

然后你只需把代码复制到 Trae 中微调和运行即可。

---

## 四、方案二：API 中转（进阶玩家）

如果你希望 Trae 能直接调用 Hermes，可以用 **LiteLLM** 等开源工具做 API 转发。

### 架构示意

```
Hermes Agent → LiteLLM（中转） → OpenAI 格式 API → Trae 自定义模型
```

### 操作步骤

```bash
# 1. 启动 Hermes API 服务
hermes api

# 2. 安装并配置 LiteLLM
pip install litellm

# 3. 将 Hermes 转发为标准 OpenAI 格式
litellm --model openai/hermes --api_base http://localhost:8000
```

### 在 Trae 中配置

```
打开 Trae → 设置 → 模型 / API 配置
├── 选择「自定义 API」
├── 接口地址 → http://localhost:8000/v1
├── API Key → Hermes 内配置的 Key
├── 模型 → 选择你原本在用的版本
└── 打开「持久上下文」→ 重启生效
```

> ⚠️ 注意：此方案依赖 LiteLLM 中转，稳定性取决于你的网络和服务器配置。新手建议从方案一入手。

---

## 五、集成后的效果对比

### 使用前 vs 使用后

| 场景 | 纯 Trae | Trae + Hermes |
|------|:-------:|:-------------:|
| 每次新会话需要重新描述项目背景 | ❌ 从头说起 | ✅ 自动加载记忆 |
| 多文件项目的代码风格一致性 | ❌ 随机性大 | ✅ 自动遵循过往风格 |
| 复杂任务需要一步步引导 | ❌ 手把手教 | ✅ 自动拆解执行 |
| 隔几天做同类功能 | ❌ 从零再来 | ✅ 调用已沉淀的技能 |
| 跨工具切换不丢失上下文 | ❌ 各自为政 | ✅ 统一记忆 |
| 知识积累和复用 | ❌ 聊完就忘 | ✅ 越用越强 |

### 效率提升

> 根据社区反馈，集成 Hermes 后：
> - 重复性研究任务效率提升 **40-60%**
> - 复杂项目理解时间缩短 **50%+**
> - 多文件跨模块修改更连贯，**减少 30% 以上的反复调试**

---

## 六、最佳实践与使用技巧

### 技巧 1：让 Hermes 做规划，Trae 做执行

这是最有效的分工方式：

```
Hermes 负责                  Trae 负责
├── 需求理解与分析            ├── 代码编辑
├── 技术方案设计              ├── 实时预览
├── 代码框架生成              ├── 调试排错
├── 任务拆解排序              ├── 运行测试
├── 经验沉淀为 Skill          └── Git 管理
└── 跨项目记忆管理
```

### 技巧 2：重视 Skill 沉淀

每当 Hermes 完成一个复杂任务后，主动确认 Skill 的生成：

```
Hermes: "检测到这是一个复杂任务，是否要保存为 Skill？"
→ 回答 "是" ✅
→ 后续类似任务直接调用，效率翻倍
```

### 技巧 3：定期整理记忆

```
# 查看当前记忆
hermes memory

# 手动清理过期信息
hermes memory --clean

# 查看已沉淀的技能
hermes skills list
```

### 技巧 4：善用跨项目记忆

Hermes 的记忆是跨项目的。你可以在 Trae 中做 A 项目时，让 Hermes 参考 B 项目的经验：

```
"参考我在 '订单管理系统' 项目中使用的分页方案，
为当前项目设计一个类似的后端分页接口"
```

---

## 七、常见问题

### Q1: 集成后 Trae 会不会变慢？

**答**：不会。Hermes 是独立运行的前置助手，与 Trae 完全解耦。Trae 的性能不受任何影响。

### Q2: 需要额外付费吗？

**答**：不需要。Hermes Agent 100% 开源免费（MIT 协议），你仍然使用 Trae 中原有的模型（免费或付费都可以），不产生任何额外费用。

### Q3: Hermes 的模型选择和 Trae 需要一致吗？

**答**：建议一致，但不是必须的。你可以让 Hermes 用本地免费模型（如 Ollama），而 Trae 用原有的云端模型，互不影响。

### Q4: Hermes 生成的代码能直接在 Trae 中运行吗？

**答**：可以。Hermes 输出的代码是标准的 Python / JS / Java 等代码文件，直接复制到 Trae 中即可编辑和运行。方案一推荐手动复制，方案二可实现自动流转。

### Q5: 集成后怎么切换回原来模式？

**答**：随时可以。Hermes 是独立运行的，不集成时 Trae 完全不受影响。停止 Hermes 服务后，一切恢复如初。

### Q6: 会不会有数据安全风险？

**答**：Hermes 完全本地运行，数据存储在你的本机 SQLite 数据库中，不会经过第三方服务器。如果你的 Trae 使用云端模型，两者的数据流独立，互不干扰。

### Q7: 需要什么硬件配置？

**答**：Hermes Agent 自身非常轻量。如果使用本地模型（Ollama），建议 16GB 内存以上；如果使用云端模型 API（如 DeepSeek/GLM），8GB 内存即可流畅运行。

---

## 总结

```
TRAE + Hermes Agent = 最佳组合

TRAE (执行)         +  Hermes (记忆)      =  完整 AI 开发体验
├── 代码编辑              ├── 跨会话记忆           ├── 不复忘
├── 调试运行              ├── 技能自动沉淀         ├── 越用越强
├── 实时预览              ├── 任务智能拆解         ├── 不迷茫
└── 部署发布              └── 经验知识复用         └── 不重复

不换工具、不换模型、不花一分钱
```

---

> **相关链接**
> - Hermes Agent 官网：[https://hermes-agent.org](https://hermes-agent.org)
> - GitHub 仓库：[https://github.com/NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent)
> - TRAE SOLO 中国版：[https://solo.trae.cn](https://solo.trae.cn)
