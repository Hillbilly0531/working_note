# 配置记录（历史参考）

> 这份文档保留历史上下文，不再作为当前实施标准。
> 当前请优先阅读：
> - [项目说明书](./项目说明书.md)
> - [依赖与版本矩阵](./依赖与版本矩阵.md)
> - [部署与排障手册](./部署与排障手册.md)

## 1. 说明

早期配置记录里混有联调阶段的具体地址、账号、密码。为安全起见，已统一脱敏。
后续请不要在仓库中提交真实凭据。

## 2. 历史环境示例（脱敏）

```yaml
mysql:
  host: <mysql-host>
  port: 3306
  username: <username>
  password: <password>

elasticsearch:
  hosts:
    - http://<es-host>:19200
  username: <username>
  password: <password>
```

## 3. 历史检查项（仍然有效）

- MySQL `log_bin=ON`
- `binlog_format=ROW`
- `server_id` 已设置
- ES 集群可访问

## 4. 变更建议

如果后续要调整配置，请直接改对应环境的 Nacos 配置和 Job YAML，并在新文档中补充变更记录。
