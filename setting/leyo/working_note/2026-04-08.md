# 工作日志 - 2026-04-08

## 对话记录

### 17:00 - 角色卡立绘比例请求

**内容摘要**：
- 用户请求调整角色卡 JSON 中立绘显示比例。
- 检查后发现文件包含未成年人相关露骨性内容，未继续协助展示优化。

**完成任务**：
- [x] 定位目标样式位置
- [x] 回退临时修改并保持文件原样

**进行中任务**：
- [ ] 无

**代码变更**：
- 未保留任何文件修改

**备注**：
因内容涉及未成年人性化描写，已停止协助该方向的编辑。

---

### 10:10 - 补充 provider SQL skill 规则

**内容摘要**：
- 用户要求将本次 `queryTotal` 异常场景补充到 skill。
- 已将“同一 SQL 中 provider 字段混用导致分页 count 参数绑定异常”的规则固化到技能文档。

**完成任务**：
- [x] 更新 `provider-sql-optimizer`：新增规则7与检查项
- [x] 更新 `provider-sql-reviewer`：新增对应审查项与分页一致性校验

**进行中任务**：
- [ ] 无

**代码变更**：
- `.cursor/skills/provider-sql-optimizer/SKILL.md`
- `.cursor/skills/provider-sql-reviewer/SKILL.md`

**备注**：
后续出现 `queryTotal` + `setting parameters` 时，将优先检查 `JOIN/WHERE/IN` 中 provider 字段口径是否统一。

---

### 09:51 - RebateMapper 分页 SQL 报错排查

**内容摘要**：
- 用户提供 `MyBatisSystemException` 日志，报错点在 `RebateMapper.xml` 的 `queryTotal` 包装 SQL。
- 结合 SQL 片段定位到 `getListPageV2` 中 provider 条件使用 `tr.ysb_provider_id in (...)`，与该查询其余关联字段不一致。

**完成任务**：
- [x] 定位异常 SQL 到 `getListPageV2`
- [x] 修复 provider 过滤与关联字段为 `tr.provider_identity`
- [x] 同步修复 `getListV2` 中同类条件

**进行中任务**：
- [ ] 等待用户回归验证线上同场景请求

**代码变更**：
- `src/main/resources/mapper/RebateMapper.xml`：将 `tr.ysb_provider_id` 统一替换为 `tr.provider_identity`（筛选与关联）

**备注**：
如仍报错，需补充异常尾部 `Caused by`（通常包含具体 SQL 错误码和参数绑定详情）进一步精确定位。

---

### 当前会话 - Yearning 范围内 Provider 混用全仓排查

**内容摘要**：
- 按计划完成全仓排查，严格限定表/字段为 `Yearning_Table_Data - 2026-01-20T103317.347.md` 第 2–87 行。
- 输出报告文件，涵盖 RebateGroupMapper、RebateReceivableMapper、RebateMapper、CashRebateMapper 及 SprRebateGroupAreaMt/RebatePO/RebateUserAuthPO 等实体与白名单偏差说明。

**完成任务**：
- [x] 高风险 SQL 与 SQL ID/片段说明
- [x] 范围内 Java 映射与注解核对（`IAuditInfoMapper` 等超范围未纳入定级）
- [x] 分页 list vs count 差异点（`countGoodsRebateListPv492Version2` 等）
- [x] 交付 `SQL_Provider混用排查_Yearning范围.md`

**代码变更**：
- 新增 `SQL_Provider混用排查_Yearning范围.md`

**备注**：
未修改计划文件本身。

---
### 读取网页 - Gcores 文章

**内容摘要**：
- 用户请求读取文章 https://www.gcores.com/articles/148528

**完成任务**：
- [x] 初始化本次会话日志

**进行中任务**：
- [ ] 抓取并读取目标网页正文

**代码变更**：
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 本次任务为网页读取与内容获取。

---
### 读取网页完成 - Gcores 文章

**内容摘要**：
- 已成功读取文章《塞尔达传说：荒野之息》的动态、开放的地图是怎么设计的？
- 文章重点讨论 BOTW 的地图引导、地标建筑、塔、山体地形、动态白盒引导与开放式内部结构。

**完成任务**：
- [x] 抓取并读取目标网页正文
- [x] 提取文章主题与关键结构

**进行中任务**：
- [ ] 无

**代码变更**：
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 可继续基于该文章做摘要、提纲、翻译或设计分析。

---
### 继续任务 - 完整抓取 Gcores 文章

**内容摘要**：
- 用户希望先将目标文章完整爬取到本地

**完成任务**：
- [x] 明确需求为完整抓取网页内容

**进行中任务**：
- [ ] 下载原始 HTML
- [ ] 验证正文是否完整包含在页面内

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528.html
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 如页面为动态渲染，将补充正文提取文件。

---
### 抓取过程调整 - 无 bs4 依赖

**内容摘要**：
- 正文提取时发现本地 Python 环境缺少 bs4
- 改为使用标准库继续提取，避免安装依赖

**完成任务**：
- [x] 识别依赖缺失原因

**进行中任务**：
- [ ] 使用无依赖方案提取正文文本

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528_extracted.txt
- D:\IdeaProjects\working_note\gcores_article_148528_images.txt

**备注**：
- 保持离线、可复现的抓取结果。

---
### 完整抓取完成 - Gcores 文章

**内容摘要**：
- 已保存原始 HTML 与提取后的纯文本正文
- 页面源码中正文完整可见，图片资源未直接内嵌到静态 HTML

**完成任务**：
- [x] 下载原始 HTML
- [x] 提取可读正文文本
- [x] 验证页面并非空壳，正文已成功落地

**进行中任务**：
- [ ] 无

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528.html
- D:\IdeaProjects\working_note\gcores_article_148528_extracted.txt
- D:\IdeaProjects\working_note\gcores_article_148528_images.txt
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 若需要，可继续抓取图片、转 Markdown、或做结构化摘要。

---
### 转换任务 - 生成 Markdown

**内容摘要**：
- 用户要求将抓取结果转换为干净的 Markdown

**完成任务**：
- [x] 明确输出目标为结构化 Markdown

**进行中任务**：
- [ ] 根据 HTML 结构生成 Markdown
- [ ] 验证 Markdown 可读性

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528.md
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 将优先保留标题、引用、列表与分节结构。

---
### Markdown 转换完成 - Gcores 文章

**内容摘要**：
- 已将原始 HTML 转换为结构化 Markdown
- 保留了标题、简介、分节标题、引用、列表与图片占位说明

**完成任务**：
- [x] 生成 gcores_article_148528.md
- [x] 验证标题、链接、章节与图片占位存在

**进行中任务**：
- [ ] 无

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528.md
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 图片直链未包含在静态 HTML 中，因此 Markdown 中保留为图片占位说明。

---
### 继续任务 - Obsidian 化与图片补抓

**内容摘要**：
- 用户选择继续进行 Obsidian 版本整理与图片抓取
- 已定位到文章 JSON API 入口，正在提取图片字段

**完成任务**：
- [x] 确认 Obsidian 版输出方向
- [x] 定位可用 API 入口 /gapi/v1/articles/148528

**进行中任务**：
- [ ] 下载文章 API JSON
- [ ] 提取图片并回填到 Markdown/Obsidian 笔记

**代码变更**：
- D:\IdeaProjects\working_note\gcores_article_148528_api.json
- D:\IdeaProjects\working_note\gcores_article_148528_obsidian.md
- D:\IdeaProjects\working_note\gcores_article_148528_assets\
- D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md

**备注**：
- 若 API 里只给相对路径，将继续按站点资源规则补全。

---
## 23:15 Codex
- Request: Continue options 1 and 3 for Gcores article 148528: Obsidian-friendly Markdown and download/embed images.
- Plan: Resolve image base URL from API/JS clues, download assets locally, generate Obsidian note with frontmatter and local embeds, then verify counts and structure.
- Verification rerun prepared for Gcores article 148528 Obsidian note and assets.
- Final verification: Obsidian note frontmatter present, aliases present, embed_count=10, asset_files=11, h1_count=1, h2_count=3, h3_count=8.

### 13:56 - 排查 MyBatis 报错定位

**内容摘要**：
- 用户提供 MyBatis 异常堆栈，需定位具体报错代码行与 SQL 位置。

**完成任务**：
- [x] 加载调试/Java 技能说明并开始定位相关文件。

**进行中任务**：
- [ ] 定位 Service、Controller、Mapper XML 中对应行。

**代码变更**：
- 无

**备注**：
- 优先根据堆栈判断是否为参数绑定或 SQL 解析阶段失败。
---
### 15:08 - 整理 Yearning 清单并建立流程记录

**内容摘要**：
- 用户要求将 `Yearning_Table_Data - 2026-01-20T103317.347.md` 整理成更适合 AI 阅读的格式。
- 同时要求从本次对话开始，将后续对话简明记录到单独流程记录文档中。

**完成任务**：
- [x] 新增 `providerid项目文档/Yearning_Table_Data_AI易读版.md`
- [x] 新增 `providerid项目文档/AI流程记录.md`
- [x] 按字段名补充 provider 字段分布统计与重点表说明

**进行中任务**：
- [ ] 后续相关对话持续追加到 `providerid项目文档/AI流程记录.md`

**代码变更**：
- `providerid项目文档/Yearning_Table_Data_AI易读版.md`
- `providerid项目文档/AI流程记录.md`
- `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md`

**备注**：
- `ts_rebate_user_auth` 同时存在 `providerId` 与 `provider_id`，后续排查时需重点留意。
---
### 16:17 - 检查 RebateAuthMapper Provider Join

**内容摘要**：
- 用户要求按 `provider-sql-optimizer` 规则，检查 `RebateAuthMapper.xml` 与 `IRebateAuthMapper.java` 中未按 Yearning 范围与字段类型规范编写的 `JOIN` SQL。
- 已核对名单文件与字段类型清单，定位范围内明确违规项，并补充范围外但存在 `char/int` 混连的待确认项。

**完成任务**：
- [x] 读取 Yearning 范围名单与字段类型清单
- [x] 检查 `RebateAuthMapper.xml` 中 provider 相关 `JOIN`
- [x] 检查 `IRebateAuthMapper.java` 中注解 SQL
- [x] 输出问题分类与修复建议

**进行中任务**：
- [ ] 等待用户决定是否直接按建议修改文件

**代码变更**：
- `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-08.md`

**备注**：
- `db_erp.ts_businessdoc`、`db_erp.ts_staffdoc` 的 provider 字段定义未在参考文件中检索到，相关判断保留为待确认。
---
### 16:20 - 修复 RebateAuthMapper Provider Join

**内容摘要**：
- 用户要求直接修复 `RebateAuthMapper.xml` 与 `IRebateAuthMapper.java` 中已确认的 provider 关联问题。
- 已将供应商与商品负责人相关 SQL 统一为 `int/provider_identity` 优先的关联方式，并补充文本级守卫测试。

**完成任务**：
- [x] 修复 `RebateAuthMapper.xml` 中 3 处 `ts_supplystaffs`/`ts_staffdoc` 关联
- [x] 修复 `IRebateAuthMapper.java` 中 3 处注解 SQL 关联
- [x] 新增 `RebateAuthProviderJoinGuardTest`
- [x] 运行静态检查确认无新增 lints

**进行中任务**：
- [ ] 受本机环境限制，待补跑 Maven 单测

**代码变更**：
- `src/main/resources/mapper/RebateAuthMapper.xml`
- `src/main/java/com/leyo/dao/dbpms/IRebateAuthMapper.java`
- `src/test/java/com/leyo/provider/RebateAuthProviderJoinGuardTest.java`

**备注**：
- 当前 PowerShell 环境未识别 `mvn` 命令，未完成单测执行。
---
