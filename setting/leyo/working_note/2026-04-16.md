# 工作日志 - 2026-04-16

## 对话记录

### 15:48 - 排查 lunar-rebate-service 中 nacos-client 版本配置位置

**内容摘要**：
- 排查 `lunar-rebate-service` 仓库内 `nacos-client` 的版本配置来源。
- 检查根 `pom.xml`、子模块 `pom.xml`、`bootstrap.yml` 和代码中的 `nacos` 相关引用。
- 尝试通过 Maven 依赖树和公司私服定位父 POM 中的真实版本定义。

**完成任务**：
- [x] 确认项目为 Maven 多模块工程，根工程父 POM 为 `com.leyo:leyo-springboot-parent:3.0.0-SNAPSHOT`
- [x] 确认当前仓库未显式声明 `com.alibaba.nacos:nacos-client`
- [x] 确认 `lunar-rebate-service-core` 通过 `com.leyo:leyo-spring-boot-svc-starter` 等内部 starter 间接引入 Nacos 的可能性很高
- [x] 确认 `bootstrap.yml` 中只有 Nacos 运行配置，没有版本配置
- [x] 发现本地缺少 Maven `settings.xml`，Maven 无法解析父 POM
- [x] 尝试访问公司私服中的父 POM 元数据，返回 401 未授权

**进行中任务**：
- [ ] 向用户反馈当前仓库中的实际配置入口和上游可能的版本定义位置

**代码变更**：
- 新增工作日志文件 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-16.md`

**备注**：
- `lunar-rebate-service-core` 代码中存在对 `com.alibaba.nacos.common.utils.StringUtils`、`CollectionUtils` 的直接引用，升级 `nacos-client` 时需要关注兼容性。

---
### 20:00 - 排查 IDEA 启动时报 SpringApplication cannot be resolved

**内容摘要**：
- 用户在 IDEA 中启动 `ServerApplication` 时，运行期抛出 `Unresolved compilation problem: SpringApplication cannot be resolved`。
- 检查后确认不是源码错误，而是 `target/classes/com/leyo/ServerApplication.class` 为错误编译产物，内部包含 `Unresolved compilation problem` stub。
- 继续排查 Maven 编译环境，发现命令行 Maven 默认运行在 JRE 上，会报 `No compiler is provided in this environment`。
- 通过显式设置 `JAVA_HOME=D:\Program Files\Java\jdk1.8.0_361` 后执行 `clean compile`，重新生成正确 class。

**完成任务**：
- [x] 确认 `ServerApplication.java` 源码本身无误
- [x] 确认旧的 `target/classes/com/leyo/ServerApplication.class` 包含 `Unresolved compilation problem` stub
- [x] 确认 Maven 在错误 JRE 环境下无法编译
- [x] 使用正确 JDK 成功执行 `clean compile`
- [x] 确认重新生成后的 `ServerApplication.class` 已不再包含 stub

**进行中任务**：
- [ ] 向用户说明 IDEA 中需要调整的 JDK / Maven 配置与下一步启动方式

**代码变更**：
- 未修改业务源码
- 重新生成 `target/` 编译产物

**备注**：
- Maven 编译过程中 `git-commit-id-plugin` 仍会输出远程 Git 认证失败日志，但不阻塞本次 `compile` 成功。

---
### 19:46 - 启用本地 CDC 任务开关

**内容摘要**：
- 按用户要求，将本地样例 CDC 任务 `pcCashCowFlow` 的 `enabled` 开关改为启用。
- 复核配置已生效，并检查 Git 跟踪状态。

**完成任务**：
- [x] 将 `src/main/resources/cdc/jobs/pc-cash-cow-flow.yml` 中的 `enabled` 从 `false` 改为 `true`
- [x] 确认文件当前为 `enabled: true`
- [x] 确认 Git 当前未跟踪该文件

**进行中任务**：
- [ ] 等待用户决定是否继续启动应用并观察 CDC 运行日志

**代码变更**：
- 修改 `src/main/resources/cdc/jobs/pc-cash-cow-flow.yml`

**备注**：
- 当前 `git ls-files` 对该文件无输出，`git status --short` 显示为 `??`，说明该任务 YAML 目前不在 Git 跟踪列表内。

---
### 18:24 - 审查 solar-openapi 中 CDC 到 ES 的实现链路

**内容摘要**：
- 审查 `solar-openapi` 仓库中是否存在通过 CDC 同步到 Elasticsearch 的实现。
- 检查 Maven 依赖、`com.leyo.cdc` 包、任务 YAML、SQL 与 ES mapping 资源。
- 重点确认真实执行链路、默认启用状态，以及可能影响功能有效性的实现风险。

**完成任务**：
- [x] 确认项目引入 `flink-connector-mysql-cdc`，并存在 `com.leyo.cdc` 相关实现
- [x] 确认启动链路会扫描 `classpath*:cdc/jobs/*.yml` 并异步启动已启用的 CDC Job
- [x] 确认核心数据链路为 MySQL Binlog -> Flink CDC -> Debezium JSON -> 主键提取 -> SQL 回查 -> ES Bulk 写入
- [x] 确认示例任务 `pc-cash-cow-flow` 配套 SQL 与 ES Mapping 资源齐全
- [x] 识别到实现风险：低流量场景缓冲区可能长期不 flush、ES Bulk 200 响应下的局部失败未校验、DELETE 事件被忽略

**进行中任务**：
- [ ] 向用户整理最终结论与关键代码定位

**代码变更**：
- 未修改仓库业务代码

**备注**：
- 当前仓库确实实现了 CDC 到 ES，但更准确地说是“CDC 事件触发 + 按主键 SQL 回查 + ES Upsert”模式，不是直接把 binlog 行原样写入 ES。
- 尝试执行 `LocalCdcJobDefinitionLoaderTest`、`SpringCdcJobDefinitionResolverTest`、`DebeziumEventParserTest`，但因父 POM `com.leyo:leyo-erp-spring-boot-starter-parent:1.0.22.RELEASE` 需要从公司私服下载且返回 `401 Unauthorized`，本地未能完成测试验证。

---
### 18:36 - 为 CDC 到 ES 链路补单元测试并手动验证

**内容摘要**：
- 根据“验证 CDC 到 ES 是否实现成功”的目标，补充核心链路单元测试。
- 新增事件路由测试与 ES sink HTTP 请求测试，避免只验证配置解析。
- 使用已有 `target/solar-openapi.jar` 中的依赖和本地缓存 JUnit，手动编译并运行新增测试，绕过 Maven 私服父 POM `401` 问题。

**完成任务**：
- [x] 新增 `PrimaryKeyRoutingFlatMapFunctionTest`，覆盖 insert/update/snapshot/delete 路由行为
- [x] 新增 `LookupBatchingEsSinkTest`，覆盖 `_bulk` 请求体、缺失 documentId 跳过、模板同步与 Basic Auth
- [x] 手动编译相关源码子集与新增测试
- [x] 通过 `org.junit.runner.JUnitCore` 运行新增 6 个测试，结果全部通过

**进行中任务**：
- [ ] 向用户整理哪些行为已被测试证明、以及当前验证边界

**代码变更**：
- 新增 `src/test/java/com/leyo/cdc/event/PrimaryKeyRoutingFlatMapFunctionTest.java`
- 新增 `src/test/java/com/leyo/cdc/sink/LookupBatchingEsSinkTest.java`

**备注**：
- 手动运行结果：`OK (6 tests)`。
- 运行时出现 `SLF4J: Failed to load class "org.slf4j.impl.StaticLoggerBinder"` 警告，但不影响本次单元测试执行结果。

---
