# 工作日志 - 2026-04-02

## 对话记录

### 10:27 - 分析启动日志与 CDC 状态

**内容摘要**：
- 分析 `err.log` 启动日志，确认 Spring Boot Web 服务是否正常启动，并判断 Flink CDC 与多任务 CDC 是否真正生效。

**完成任务**：
- [x] 确认应用主服务已成功启动
- [x] 确认启动过程中存在 P6Spy 自定义日志类缺失
- [x] 确认 `sys_config` 表不存在导致 CDC 配置读取报错
- [x] 确认当前未监听任何表

**进行中任务**：
- [ ] 向用户说明问题原因与后续排查建议

**代码变更**：
- 新增工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
应用最终启动成功，但 CDC 相关日志显示未启用单任务 CDC，且多任务 CDC 配置文件未被实际加载，当前没有表监听在运行。

---
### 16:08 - 修复销售返利门店类型误匹配

**内容摘要**：
- 修复 `RebateReceiveDetailMapper.xml` 中 4 个销售返利 SQL 的门店类型模糊匹配问题，不再在 SQL 中使用 `LOCATE` 判断 `clientType/storeType`，改为将字段透出后在 `RebateReceiveDetailService` 外层做精确过滤。

**完成任务**：
- [x] 为 `RebateReceiveDetailInfoDo` 增加 `clientType`、`storeType` 字段
- [x] 调整 4 个 SQL，去掉 `LOCATE(tc.ClientType, IF(area.stor_type='-1',tc.ClientType,area.stor_type)) > 0`
- [x] 在 `filterReceiveDetailList` 中新增门店类型精确匹配逻辑
- [x] 在外层过滤后按明细业务键去重，避免 `area` 多行导致重复明细
- [x] 新增回归测试 `RebateReceiveDetailServiceTest`

**进行中任务**：
- [ ] 等待用户决定是否需要继续补数据库侧验证或联调验证

**代码变更**：
- 修改 `D:\IdeaProjects\lunar-rebate-service\lunar-rebate-service-core\src\main\java\com\leyo\lunar\rebate\core\dao\model\dbo\RebateReceiveDetailInfoDo.java`
- 修改 `D:\IdeaProjects\lunar-rebate-service\lunar-rebate-service-core\src\main\java\com\leyo\lunar\rebate\core\service\dbservice\bizdc\received\RebateReceiveDetailService.java`
- 修改 `D:\IdeaProjects\lunar-rebate-service\lunar-rebate-service-core\src\main\resources\mapper\bizdc\RebateReceiveDetailMapper.xml`
- 新增 `D:\IdeaProjects\lunar-rebate-service\lunar-rebate-service-core\src\test\java\com\leyo\lunar\rebate\core\service\dbservice\bizdc\received\RebateReceiveDetailServiceTest.java`
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
- 本机已找到 Maven 可执行文件，但执行单测时被父 POM `com.leyo:leyo-springboot-parent:3.0.0-SNAPSHOT` 无法解析阻断，暂未取得完整测试通过证据。

---

### 15:28 - CDC Job 改为复用 Nacos/Spring 连接配置

**内容摘要**：
- 按计划完成 CDC Job 配置瘦身，将任务 YAML 从“包含完整 MySQL/ES 连接信息”调整为“只保留任务定义”，并在启动阶段自动从 Spring/Nacos 中补全 `slave_1` 数据源与 `spring.data.elasticsearch` 配置。

**完成任务**：
- [x] 新增 Spring/Nacos 到 CDC Definition 的配置补全桥接层
- [x] 拆分 CDC Job 的加载期校验与运行期最终校验
- [x] 将配置补全接入 `CdcJobBootstrapService` 启动流程
- [x] 精简 `pc-cash-cow-flow.yml`，仅保留任务定义字段
- [x] 补充 resolver 与精简 YAML 加载测试
- [x] 更新根目录 `README.md` 说明新的配置来源
- [x] 通过 `mvn clean test`
- [x] 通过 `mvn clean package`

**进行中任务**：
- [ ] 等待用户决定是否继续把 MySQL/ES 配置来源做成可切换数据源键，而不是固定读取 `slave_1`

**代码变更**：
- 修改 `D:\IdeaProjects\solar-sql-self-chk\src\main\java\com\leyo\cdc\config\CdcJobDefinition.java`
- 修改 `D:\IdeaProjects\solar-sql-self-chk\src\main\java\com\leyo\cdc\config\LocalCdcJobDefinitionLoader.java`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\main\java\com\leyo\cdc\config\SpringCdcJobDefinitionResolver.java`
- 修改 `D:\IdeaProjects\solar-sql-self-chk\src\main\java\com\leyo\cdc\service\CdcJobBootstrapService.java`
- 修改 `D:\IdeaProjects\solar-sql-self-chk\src\main\resources\cdc\jobs\pc-cash-cow-flow.yml`
- 修改 `D:\IdeaProjects\solar-sql-self-chk\src\test\java\com\leyo\cdc\config\LocalCdcJobDefinitionLoaderTest.java`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\test\java\com\leyo\cdc\config\SpringCdcJobDefinitionResolverTest.java`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\test\resources\cdc\jobs\minimal-cdc-job.yml`
- 修改 `D:\IdeaProjects\solar-sql-self-chk\README.md`
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
- 当前实现默认从 `spring.datasource.dynamic.datasource.slave_1.*` 解析 MySQL 连接信息，并自动去掉 `jdbc:p6spy:` 前缀。
- 当前实现默认从 `spring.data.elasticsearch.*` 读取 ES 地址和认证信息，并会自动为未带协议的 host 补全 `http://`。
- `git-commit-id-plugin` 仍会输出远端 Git 认证告警，但不影响本次 `test/package` 成功。

---

### 11:48 - 更新项目工作区 JDK 路径

**内容摘要**：
- 按用户确认，将项目工作区的 Java 相关配置统一切换为 `D:\Program Files\Java\jdk1.8.0_361`，以修复 Cursor Java 语言服务和 Maven 终端使用错误 JRE 路径的问题。

**完成任务**：
- [x] 更新 `java.configuration.runtimes[].path`
- [x] 重新设置 `java.jdt.ls.java.home`
- [x] 更新 Maven 终端环境变量 `JAVA_HOME`

**进行中任务**：
- [ ] 等待用户重载 Cursor 窗口验证 Java 扩展恢复正常

**代码变更**：
- 修改 `D:\IdeaProjects\solar-sql-self-chk\.vscode\settings.json`
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
当前工作区配置已不再使用错误的 `D:\Program Files\java1.8`，后续若仍提示旧错误，通常只需执行一次 `Reload Window` 或重启 Cursor 使 Java 扩展重新加载配置。

---

### 11:46 - 核对本机 JDK 8 安装目录

**内容摘要**：
- 根据用户提供的新路径 `D:\Program Files\Java\jdk1.8.0_361` 检查目录内容与版本信息，确认该目录是完整可用的 JDK 8 安装目录。

**完成任务**：
- [x] 读取 `release` 文件确认版本为 `1.8.0_361`
- [x] 确认 `bin` 目录存在 `java.exe` 与 `javac.exe`
- [x] 执行 `java -version` 与 `javac -version` 验证可用性

**进行中任务**：
- [ ] 视用户需要将 Cursor 工作区 Java 配置改为该 JDK 路径

**代码变更**：
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
此前报错的原因不是没有 JDK，而是工作区配置写成了 `D:\Program Files\java1.8`；正确可用的路径应为 `D:\Program Files\Java\jdk1.8.0_361`。

---

### 11:40 - 修复 Cursor Java JDT 配置错误

**内容摘要**：
- 排查 `java.jdt.ls.java.home` 指向 `D:\Program Files\java1.8` 的报错，确认该目录仅包含 Java 8 运行时而非完整 JDK，并清理工作区中的错误配置项。

**完成任务**：
- [x] 读取 Cursor 用户设置与项目工作区设置
- [x] 确认 `D:\Program Files\java1.8` 缺少 `javac.exe`
- [x] 从 `D:\IdeaProjects\solar-sql-self-chk\.vscode\settings.json` 中移除 `java.jdt.ls.java.home`

**进行中任务**：
- [ ] 向用户说明仍需安装真实 JDK 才能完整支持 Java 编译与 Maven

**代码变更**：
- 修改 `D:\IdeaProjects\solar-sql-self-chk\.vscode\settings.json`
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
当前机器上未检索到可用的 `javac.exe`，说明尚未安装完整 JDK；移除错误配置后，可避免 JDT LS 继续使用错误路径，但若要正常构建 Java 项目，仍需安装 JDK 并更新 `java.configuration.runtimes` / `JAVA_HOME`。

---

### 11:32 - 安装 Java 开发相关 Cursor 扩展

**内容摘要**：
- 根据用户提供的截图，核对当前 Cursor 已安装扩展，并用命令行再次执行安装确认 Java 开发相关扩展状态。

**完成任务**：
- [x] 检查当前已安装的 Cursor 扩展列表
- [x] 确认 `Extension Pack for Java` 已安装
- [x] 确认 `Spring Boot Extension Pack` 已安装
- [x] 确认 `IntelliJ IDEA Keybindings` 已安装

**进行中任务**：
- [ ] 向用户同步安装确认结果

**代码变更**：
- 未修改业务代码
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
`cursor --install-extension` 返回结果显示 3 个目标扩展均已安装；如需强制升级到最新版本，可追加 `--force` 重新安装。

---

### 11:15 - 实现通用 Flink CDC 多作业框架

**内容摘要**：
- 基于本地 YAML 配置实现通用 Flink CDC 多作业框架，支持按任务启用、仅处理 insert/update、按参数控制 `INITIAL` 与 `LATEST_OFFSET` 启动模式，并以 `pc_cash_cow_flow` 作为落地样例。

**完成任务**：
- [x] 新增 CDC Job 配置模型与本地 YAML 加载器
- [x] 新增应用启动自动拉起 enabled 任务的启动器
- [x] 新增 Debezium 事件解析、insert/update 过滤与主键提取逻辑
- [x] 新增按主键批量回查 SQL 并写入 ES 的 Sink
- [x] 新增 `pc_cash_cow_flow` 的 YAML、SQL、ES mapping 样例
- [x] 新增核心单测骨架与日志输出

**进行中任务**：
- [ ] 等待具备私服权限后执行 Maven 编译与测试验证

**代码变更**：
- 修改 `D:\IdeaProjects\solar-sql-self-chk\pom.xml`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\main\java\com\leyo\cdc\**`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\main\resources\cdc\**`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\test\java\com\leyo\cdc\**`
- 新增 `D:\IdeaProjects\solar-sql-self-chk\src\test\resources\cdc\jobs\pc-cash-cow-flow.yml`

**备注**：
已将 Flink CDC 依赖基线切换为兼容 `Flink 1.20.1` 的 `3.4.0` 方案，但当前 Maven 构建仍被公司私服父 POM `401 Unauthorized` 阻断，尚未获得完整编译证据。

---

### 10:29 - 确认 cdc-tasks.yml 未生效原因

**内容摘要**：
- 核对 `cdc-tasks.yml` 的加载链路，确认 `CdcTaskConfig` 绑定逻辑本身存在，但 Spring Boot 没有任何地方显式导入该文件。

**完成任务**：
- [x] 确认 `cdc-tasks.yml` 不属于 Spring Boot 默认自动加载配置文件
- [x] 确认项目中不存在 `spring.config.import`、`@PropertySource` 等显式导入配置
- [x] 确认 `MultiCdcJobManager` 读到 `0` 个任务是因为 `CdcTaskConfig.tasks` 保持默认空列表

**进行中任务**：
- [ ] 向用户说明可选修复方式

**代码变更**：
- 更新工作日志 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-02.md`

**备注**：
`CdcTaskConfig` 使用了 `@ConfigurationProperties(prefix = "cdc")`，但前提是 `Environment` 中已经存在 `cdc.tasks`；当前仅有 `application.yml` 和 `application-dev.yml` 被自动加载，`cdc-tasks.yml` 没有被纳入配置源。

---
### 15:44 - SaleWithExpress ��������

**����ժҪ**��
- ���û�Ҫ�� SaleWithExpress �����������¸�Ϊ����ִ�У����������� 300��
- �����������⣬��֤ 301 �����»������� mapper ���á�

**�������**��
- [x] �޸� `SaleWithExpressServiceImpl`���� `batchUpdate(updateList)` ��Ϊ��������
- [x] ���� `SaleWithExpressServiceImplTest` ��֤���β���߼�

**����������**��
- [ ] �ȴ��û�ȷ���Ƿ���Ҫ�������ܱ��� Maven ���Ի���

**������**��
- `src/main/java/com/leyo/service/dberp/impl/SaleWithExpressServiceImpl.java`
- `src/test/java/com/leyo/service/dberp/impl/SaleWithExpressServiceImplTest.java`

**��ע**��
- ��ǰ�ն˻���ȱ�� `mvn`��δ��ֱ��ִ�� Maven �������

---
**��֤����**��
- ���ҵ����� Maven��`C:\Users\47260\.trae-cn\tools\maven\latest\bin\mvn.cmd`
- ִ�� `-Dtest=SaleWithExpressServiceImplTest test` ʱʧ�ܣ�ԭ����˽�� Nexus ������ȡ���� 401���Ǳ��δ����﷨����

---
### 任务记录 - 图片目录 OCR 提取

**内容摘要**：
- 对 `D:\IdeaProjects\mmd-maker\灵感碎片\写卡思路\图片` 下的各子文件夹图片执行 OCR。
- 按文件夹分别生成独立 Markdown 文档。
- 为流程补充可复用脚本和单元测试，并使用 Windows 原生 OCR 作为当前机器的识别后端。

**完成任务**：
- [x] 新增 `scripts/extract_image_text_to_md.py`
- [x] 新增 `scripts/windows_ocr.ps1`
- [x] 新增 `tests/test_extract_image_text.py`
- [x] 生成 `图片\提取结果\人物塑造1.md`
- [x] 生成 `图片\提取结果\人物塑造2.md`
- [x] 生成 `图片\提取结果\人物塑造3.md`
- [x] 运行单元测试通过（4/4）

**代码变更**：
- `D:\IdeaProjects\mmd-maker\scripts\extract_image_text_to_md.py`
- `D:\IdeaProjects\mmd-maker\scripts\windows_ocr.ps1`
- `D:\IdeaProjects\mmd-maker\tests\test_extract_image_text.py`

**备注**：
- PowerShell 终端直接查看中文时有编码噪音，但程序化读取确认输出 Markdown 内容为正常 UTF-8 中文。

---
### 17:10 - 整理写卡思路提取文档

**内容摘要**：
- 整理 `灵感碎片\写卡思路\图片\提取结果` 目录下的 3 份提取文档
- 清理原始提取结果中的空格化排版与不清晰结构
- 按文档实际用途重命名为更清晰的名称

**完成任务**：
- [x] 重写并优化 3 份 Markdown 文档格式
- [x] 将 `人物塑造1.md` 重命名为 `角色塑造完整模板.md`
- [x] 将 `人物塑造2.md` 重命名为 `角色塑造核心原则.md`
- [x] 将 `人物塑造3.md` 重命名为 `角色深挖提问清单.md`

**代码变更**：
- `D:\IdeaProjects\mmd-maker\灵感碎片\写卡思路\图片\提取结果\角色塑造完整模板.md`
- `D:\IdeaProjects\mmd-maker\灵感碎片\写卡思路\图片\提取结果\角色塑造核心原则.md`
- `D:\IdeaProjects\mmd-maker\灵感碎片\写卡思路\图片\提取结果\角色深挖提问清单.md`

**备注**：
原始文件内容存在明显提取排版问题，已按内容主题重新结构化整理。

---
