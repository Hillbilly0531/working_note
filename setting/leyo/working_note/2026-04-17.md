# 工作日志 - 2026-04-17

## 对话记录

### 09:59 - 评审28.28.0需求与后端开发文档

**内容摘要**：
- 审阅 `28.28.0需求文档.md` 与 `28.28.0后端开发文档.md`
- 结合现有 Java / Mapper 实现核对权限码、归档状态枚举、负责人变更提醒、快照表与日志能力
- 识别文档遗漏项、真实阻塞项以及文档中误判为阻塞的点

**完成任务**：
- [x] 读取需求文档与后端开发文档
- [x] 核对归档状态字段与 `notice_id` 现有语义
- [x] 核对权限枚举与相关表字段
- [x] 输出遗漏项、真实阻塞项与非阻塞项结论

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 新增日志文件 `D:\IdeaProjects\working_note\setting\leyo\working_note\2026-04-17.md`

**备注**：
发现多处后端开发文档将“待确认”与“现有代码已明确”混在一起，需重新区分真实阻塞；同时发现需求第2项“全品协议禁用商品上下限”未进入后端开发文档范围，批量变更负责人方案遗漏集团子协议 `notice_id`、`rebate_person_name` 以及普通协议快照表同步等关键点。

---

### 10:35 - 更新后端开发技术文档

**内容摘要**：
- 将上一轮评审结论直接回写到 `28.28.0后端开发文档.md`
- 补充范围说明、批量变更负责人/批量归档的遗漏设计、真实阻塞项和风险说明
- 明确 `notice_id` 语义、普通协议快照表同步、集团子协议附加字段同步及接口返回待确认项

**完成任务**：
- [x] 在后端开发文档中补充需求第2项范围说明
- [x] 修正批量变更负责人章节中的同步表、字段与核心流程
- [x] 修正批量归档章节中的快照表同步与状态校验说明
- [x] 重写“需要确认的信息清单”为真实阻塞项

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 更新 `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\28.28.0后端开发文档.md`

**备注**：
本次为文档更新，未涉及业务代码实现，也无对应自动化测试可运行；已通过回读文档内容确认关键章节已写入。

---

### 10:43 - 根据用户补充信息二次修订文档

**内容摘要**：
- 根据用户补充的阻塞信息，进一步收敛后端开发文档
- 将姓名对照表明确为 `db_pms.ts_userinfo`
- 将批量接口返回方式统一为失败返回 JSON
- 将批量负责人变更权限调整为新增权限码方案

**完成任务**：
- [x] 更新负责人姓名匹配规则为 `select user_id,user_name from db_pms.ts_userinfo`
- [x] 更新批量接口为复用现有导入接口风格
- [x] 更新导入失败返回方式为 JSON
- [x] 更新批量负责人变更为新增权限码方案

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 更新 `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\28.28.0后端开发文档.md`

**备注**：
用户提供的 `db_biz_dc.ts_rebate_agreement.sql` 路径已写入文档作为表结构来源说明；当前仓库内该 SQL 文件本身为空，文档中保留为“以该 SQL 文件及后续实现确认结果为准”的表述。

---

### 10:58 - 补充权限码建议与DC表关联说明

**内容摘要**：
- 根据用户提供的 SQL 片段，将 DC 协议表关联关系写入文档
- 先定义新的批量负责人变更权限码建议值，便于后续用户自行维护
- 将阻塞项进一步收缩为 DC 表最终写入字段确认

**完成任务**：
- [x] 定义建议权限码 `rebate_person_batch_edit`
- [x] 定义建议权限码 `rebate_group_person_batch_edit`
- [x] 补充 `db_biz_dc.ts_rebate_agreement.bill_code = db_pms.ts_rebate.rebate_code`
- [x] 补充 DC 负责人展示字段参考 `operator`

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 更新 `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\28.28.0后端开发文档.md`

**备注**：
当前文档已将 DC 表同步逻辑写到“按 `bill_code = rebate_code` 关联、展示字段先按 `operator` 设计”的粒度；最终落库字段仍保留为实现前最后确认项。

---

### 11:00 - 确认DC同步字段为operator

**内容摘要**：
- 根据用户最新确认，将 `db_biz_dc.ts_rebate_agreement` 的最终同步字段明确为 `operator`
- 同步移除文档里关于该字段的阻塞描述

**完成任务**：
- [x] 将 DC 负责人展示字段由“待确认”改为确定更新 `operator`
- [x] 将阻塞清单更新为当前无新增阻塞项

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 更新 `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\28.28.0后端开发文档.md`

**备注**：
当前文档已明确：批量负责人变更时，通过 `bill_code = rebate_code` 关联，同步更新 `db_biz_dc.ts_rebate_agreement.operator`。

---

### 11:05 - 收敛接口与执行策略口径

**内容摘要**：
- 根据用户最新确认，更新接口版本号、事务策略、并发策略、异步策略、数量限制和后端兜底范围

**完成任务**：
- [x] 接口版本号明确为 `pv28280`
- [x] 批量导入失败明确为整批回滚
- [x] 明确不允许并发更新
- [x] 明确不使用异步处理
- [x] 明确不限制单次导入数量
- [x] 明确第2项需求不做后端兜底

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- 更新 `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\28.28.0后端开发文档.md`

**备注**：
文档当前已基本收敛到可执行口径，仅保留“是否补充操作日志”作为建议项。

---

### 10:18 - 增加 CDC 诊断日志
**内容摘要**：
- 为 CDC 路由和 ES sink 增加诊断日志，便于判断事件是否进入 Flink、是否成功提取主键、是否触发 SQL 回查以及 ES bulk 是否有失败项。
- 先补充单元测试约束事件摘要、忽略原因、ES bulk 响应摘要和 ID 预览格式，再实现日志。
- 定点测试通过，当前可直接通过应用日志判断断点在 source、route、lookup 还是 ES。

**完成任务**：
- [x] 为 `PrimaryKeyRoutingFlatMapFunction` 增加收到事件、路由成功、忽略原因日志
- [x] 为 `LookupBatchingEsSink` 增加 sink 初始化、收到主键、flush、SQL 回查、ES bulk 摘要日志
- [x] 补充并通过相关单元测试

**代码变更**：
- `src/main/java/com/leyo/cdc/event/PrimaryKeyRoutingFlatMapFunction.java`
- `src/main/java/com/leyo/cdc/sink/LookupBatchingEsSink.java`
- `src/test/java/com/leyo/cdc/event/PrimaryKeyRoutingFlatMapFunctionTest.java`
- `src/test/java/com/leyo/cdc/sink/LookupBatchingEsSinkTest.java`
- `src/main/resources/spy.properties`

**备注**：
- Maven 运行时仍会出现 `git-commit-id-plugin` 的远端认证报错输出，但不影响本次定点测试通过。
---

### 11:20 - 拆分 V28.28.0 AI 串行开发文档
**内容摘要**：
- 按“负责人链路先闭环、归档链路后闭环”的思路，将原有后端开发文档拆成独立模块文档
- 新增总览文档，明确串行顺序、模块边界、共享口径、AI执行约束
- 新增 6 份模块开发文档，分别约束基础契约、负责人导入解析、负责人校验、负责人更新、负责人接口接入、批量归档

**完成任务**：
- [x] 创建 `AI开发拆分` 目录
- [x] 新增模块总览文档
- [x] 新增 6 份独立模块开发文档
- [x] 明确每个模块的输入、输出、改动范围、禁止事项、完成标准、自测清单

**进行中任务**：
- [ ] 无

**代码/文档变更**：
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\00-模块总览.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\01-基础契约模块开发文档.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\02-负责人批量导入解析模块开发文档.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\03-负责人批量校验模块开发文档.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\04-负责人批量更新模块开发文档.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\05-负责人接口接入与联调模块开发文档.md`
- `d:\IdeaProjects\pms_rebate\docs\28.28.0需求文档\AI开发拆分\06-批量归档模块开发文档.md`

**备注**：
- 本次仅新增文档拆分，不涉及代码实现与自动化测试
- 后续可以直接按模块文档逐个喂给 AI 串行执行
---

### 11:18 - 修复 CDC 引导类坏编译产物
**内容摘要**：
- 用户提供的 err.log 中完全没有 CDC 启动日志，进一步排查发现运行时使用的 	arget/classes/com/leyo/cdc/service/CdcJobBootstrapService.class 是 IDE 生成的坏编译产物，字节码内包含 Unresolved compilation problems。
- 该问题会导致 CDC 引导 ApplicationRunner 实际不按源码逻辑执行，因此应用能启动但 CDC 完全不启动。
- 已将 CdcJobBootstrapService 的多参数日志改为 Object[] 写法，并手工重新编译该类，确认 class 已恢复为正常的 org.springframework.boot.ApplicationRunner 实现。

**完成任务**：
- [x] 定位 err.log 中缺失 CDC 日志的根因
- [x] 确认 CdcJobBootstrapService.class 为坏编译产物
- [x] 修复日志调用兼容性并手工重编该类

**代码变更**：
- src/main/java/com/leyo/cdc/service/CdcJobBootstrapService.java

**备注**：
- 下一步需要用户重启 IDEA 进程，再看 CDC 启动日志是否恢复；若恢复，再继续判断是否卡在 MySqlSource 创建阶段。

### 11:24 - 总结 IDEA 处理坏编译产物的方法
**内容摘要**：
- 用户询问如何在 IDEA 中自行处理 Unresolved compilation problem 这类坏 class，而不是每次手工重编。
- 已总结稳定做法：优先修复 IDEA 的 Maven 导入与编译器配置，避免 IDEA 生成 Eclipse 风格的错误桩 class；并给出启动配置和缓存处理建议。

**完成任务**：
- [x] 提供 IDEA 内的长期处理方案
- [x] 说明如何避免再次生成坏 class

**备注**：
- 当前项目出现过 ServerApplication.class 和 CdcJobBootstrapService.class 被 IDEA 编成坏产物的情况，核心处理方向是避免 IDEA 增量编译污染 	arget/classes。

### 11:38 - 为 MySqlSource.build 增加自动线程栈抓取
**内容摘要**：
- 用户要求直接定位 uilder.build() 内部卡住的类。
- 已在 CdcJobRunner 中增加超时诊断线程：当 MySqlSource.builder().build() 超过 10 秒未返回时，自动打印 cdc/debezium/mysql/flink/kafka 相关线程与 cdc-job-runner 的线程栈。
- 手工编译该类时再次暴露本地 IDEA/Flink 解析状态不稳定的问题，但源码改动已完成，可通过 IDEA/Maven 重新加载后生效。

**完成任务**：
- [x] 增加 uilder.build() 超时自动线程栈抓取
- [x] 将多参数日志改为 Object[] 写法，兼容当前 logger 解析环境

**代码变更**：
- src/main/java/com/leyo/cdc/job/CdcJobRunner.java

**备注**：
- 下一步由用户在 IDEA 中重新加载 Maven 并启动，观察 10 秒后输出的线程栈，定位卡在 Flink CDC / Debezium 的具体类。

### 17:35 - provider_identity XML 全项目漏改排查（进行中）
**内容摘要**：
- 基于 `provider-identity-mybatis-xml-scope` 建立全项目检查清单并执行首轮扫描。
- 先跑官方脚本 dry-run（整文件别名绑定口径）确认“可自动替换”结果，再补充“按 SQL 块”细粒度复扫，避免因跨语句别名复用导致漏检。
- 已定位到 4 个 Mapper 中共 7 处白名单范围内旧列残留，需要进入下一步人工确认与修复。

**完成任务**：
- [x] 读取 skill 与 Yearning 白名单
- [x] 全量 Mapper.xml 关键词盘点
- [x] 官方脚本 dry-run 全仓扫描
- [x] 按 SQL 块二次审计并产出疑似漏改列表

**进行中任务**：
- [ ] 对 4 个文件的 7 处命中逐条确认是否应改为 `provider_identity`
- [ ] 与你分批同步检查结论并等待是否执行落地修复

**代码/文档变更**：
- 新增临时审计脚本 `d:\IdeaProjects\lunar-rebate-service\.cursor\tmp\provider_scope_audit.py`

**备注**：
- 当前仅做审计，尚未改动业务 Mapper；后续会按 checklist 分步汇报，不一次性倾倒结果。
