# 工作日志 - 2026-04-27

## 对话记录

### 11:44 - 修复集团子协议 provider_identity 查询报错

**内容摘要**：
- 排查 `groupCommonRebateList` 报错，定位到 `CommonProtocolMapper.xml` 中集团子协议查询仍使用 `a.ysb_provider_id` 过滤，和当前 `provider_identity` 迁移不一致。
- 排查终端栈中的 `SprRebateGroupGoods` MyBatis-Plus Wrapper 报错，定位到 LambdaWrapper 使用了 `@TableField(exist = false)` 的 `providerId` 属性。

**完成任务**：
- [x] 将集团子协议查询中 `db_pms.ts_rebate` 侧供应商关联与过滤改为 `provider_identity`。
- [x] 将 `ISprRebateGroupGoodsMapper.selectList` 的 Wrapper 条件改为 `providerIdentity` 字段。
- [x] 清理 `ISprRebateGroupGoodsMapper` 中未使用 import。

**代码变更**：
- `src/main/resources/mapper/CommonProtocolMapper.xml`
- `src/main/java/com/leyo/dao/dbpms/rebateGroup/spr/ISprRebateGroupGoodsMapper.java`

**备注**：
IDE 诊断已通过；本机 PowerShell 环境未识别 `mvn`，且仓库无 Maven Wrapper，未能执行 Maven 编译。

---

### 19:55 - 落地项目开发规则文档

**内容摘要**：
- 根据返利计算、应收明细、主从延迟、金额一致性等项目特有兜底逻辑，整理 Cursor Agent 项目规则。
- 将“开发完成后必须证明金额可校验、任务可重放、异常可追踪、延迟不误算”固化为项目级规则。

**完成任务**：
- [x] 新增项目 Agent 总入口规则。
- [x] 新增返利计算数据安全底线规则。
- [x] 在 `项目开发规范.md` 中补充已落地文件索引。

**代码变更**：
- `d:\IdeaProjects\lunar-rebate-service\.cursor\rules\agent.mdc`
- `d:\IdeaProjects\lunar-rebate-service\.cursor\rules\lunar-rebate-domain-safety.mdc`
- `d:\IdeaProjects\lunar-rebate-service\项目开发规范.md`

**备注**：
本次为文档与 Cursor rules 落地，未修改业务代码；IDE 诊断未发现新增问题。

---

### 19:59 - 补齐 Rules + Skill 落地方案

**内容摘要**：
- 按“Rules 常驻约束 + Skill 专项流程”的方案继续落地。
- 新增返利数据安全专项 Review Skill，用于 MR Review、上线前检查、金额 SQL、重算补偿等专项审查。
- 调整 `.gitignore`，默认仍忽略 `.cursor` 本地内容，但放行项目 rules 与本次新增 Skill，便于团队共享。

**完成任务**：
- [x] 新增 `.cursor/skills/rebate-data-safety-review/SKILL.md`。
- [x] 更新 `.cursor/rules/agent.mdc`，声明专项 Review 时使用该 Skill。
- [x] 更新 `.gitignore`，放行 `.cursor/rules` 与指定项目 Skill。
- [x] 更新 `项目开发规范.md` 的落地文件清单。

**代码变更**：
- `d:\IdeaProjects\lunar-rebate-service\.cursor\skills\rebate-data-safety-review\SKILL.md`
- `d:\IdeaProjects\lunar-rebate-service\.cursor\rules\agent.mdc`
- `d:\IdeaProjects\lunar-rebate-service\.gitignore`
- `d:\IdeaProjects\lunar-rebate-service\项目开发规范.md`

**备注**：
本次为规则与 Skill 文档落地，未修改业务代码；IDE 诊断未发现新增问题。

---
