# 工作日志 - 2026-04-23

## 对话记录

### 16:01 - SprRebateGroupService 无事务推送改造

**内容摘要**：
- 按用户要求，在 `SprRebateGroupServiceImpl` 中复制集团统管协议推送子公司的方法逻辑，新增无事务版本。
- 保持 `sprRebateGroupPushToAll(...)` 现状不变，仅让单个推送方法通过接口代理转发到无事务实现。

**完成任务**：
- [x] 确认 `ISprRebateGroupService` 已存在 `sprRebateGroupPushToChildCompanyNoTx(...)` 定义
- [x] 在 `SprRebateGroupServiceImpl` 中新增 `ISprRebateGroupService` 延迟注入代理
- [x] 将 `sprRebateGroupPushToChildCompany(...)` 调整为事务包装器
- [x] 新增 `sprRebateGroupPushToChildCompanyNoTx(...)` 并保留原始业务逻辑
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 无

**代码变更**：
- `src/main/java/com/leyo/service/dbpms/rebateGroup/impl/SprRebateGroupServiceImpl.java`

**备注**：
本机命令行环境未安装 `mvn`，因此未能补充 Maven 编译验证；当前仅完成 IDE 诊断与代码结构核对。

---

### 16:22 - 水上乐园侧边栏二级菜单排查

**内容摘要**：
- 对比 `水上乐园` 正则文件与 `标准模板_状态栏+侧边栏_V1.json`，排查地图栏、NPC栏、特化库导入 SEXTOP 后二级内容不显示的问题。
- 确认当前文件的一二级结构依赖运行时动态装配，而标准模板使用静态直出结构，因此平台导入后表现不一致。

**完成任务**：
- [x] 读取并比对目标文件与标准模板
- [x] 定位地图、NPC、特化三个侧边栏的二级内容生成方式差异
- [x] 识别当前实现对 `onerror` 动态初始化的依赖风险
- [x] 发现地图弹层 HTML 存在额外闭合标签问题

**进行中任务**：
- [ ] 等待用户确认是否需要我直接改成与标准模板一致的稳定写法

**代码变更**：
- 无

**备注**：
当前结论是：外层按钮和弹层骨架属于静态 HTML，所以能显示；二级菜单项、详情数据、标签页内容依赖隐藏图片 `onerror` 执行 JS 动态生成，导入平台后这段初始化未可靠执行，因此内部内容为空。

---

### 16:34 - 水上乐园侧边栏兼容性修复

**内容摘要**：
- 按用户确认，将 `水上乐园` 正则文件中的地图栏、特化库、NPC栏改为更接近标准模板的静态写法。
- 移除对 `img onerror` 动态生成二级内容的依赖，降低 SEXTOP 平台导入后的兼容性风险。

**完成任务**：
- [x] 将地图弹层改为静态地点按钮与详情切换
- [x] 将特化库改为静态 tab 与静态按钮内容
- [x] 将 NPC 数据改为静态列表页与静态详情页
- [x] 清理不再需要的 `NPC_DATA_STASH` 动态注入
- [x] 进行 JSON 解析验证
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户重新导入 SEXTOP 平台确认实际显示效果

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
本次修改属于配置型 JSON 兼容性修复，未新增自动化测试；已通过 `python -c "import json; ..."` 完成文件级 JSON 有效性校验，结果为 `JSON_OK`。

---

### 16:43 - 二开文档补充兼容性禁令

**内容摘要**：
- 按用户要求，将本次 SEXTOP 导入后二级菜单空白的问题与修复方案写入二次开发指南。
- 在文档中加入强制规则，明确禁止后续二开继续使用 `onerror` 动态创建地图/NPC/特化二级菜单。

**完成任务**：
- [x] 更新二开文档头部更新时间
- [x] 在推荐原则中新增 `onerror` 使用禁令
- [x] 在常见问题中新增“一级显示、二级空白”的排错条目
- [x] 在案例记录中补充本次水上乐园修复结论
- [x] 进行文档内容复查

**进行中任务**：
- [ ] 无

**代码变更**：
- `资料库/正则资料/正则模板/标准模板_状态栏+侧边栏_V1_二次开发指南.md`

**备注**：
文档已明确要求：地图点位、特化条目、NPC 列表/详情等二级结构必须静态直出到 `replaceString`，不得再依赖 `img onerror` 在宿主环境中动态创建。

---

### 16:50 - 水上乐园侧边栏层级与尺寸微调

**内容摘要**：
- 按用户要求，针对 `水上乐园` 现有静态侧边栏版本继续做 UI 兼容性微调，重点处理 NPC 面板过宽与地图/特化二级弹窗被遮挡的问题。
- 参考标准模板的层级策略，保留现有结构，仅通过 `z-index` 与面板宽度调整完成修复。

**完成任务**：
- [x] 对照标准模板确认本次问题属于同层弹窗层级冲突
- [x] 将地图入口层级提升到最高层
- [x] 将特化入口层级提升到最高层
- [x] 下调 NPC 弹层层级，避免遮挡地图/特化二级弹窗
- [x] 将 NPC 侧栏宽度由 `420px` 缩小为 `380px`
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台重新导入后确认实际交互效果

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
本次为最小改动修复，未重写地图/特化/NPC 结构；命令行校验结果显示 `JSON_OK True`、`FAIL_COUNT 0`，且关键字段校验为 `MAP_Z True`、`SPEC_Z True`、`NPC_MODAL_Z True`、`NPC_WIDTH True`。

---

### 16:34 - 水上乐园 NPC 面板高度下调

**内容摘要**：
- 根据用户最新反馈，继续缩小 `NPC` 栏位占屏比例。
- 确认问题主要来自面板高度使用了整屏 `100vh`，因此改为 70% 视口高度。

**完成任务**：
- [x] 定位 `NPC_CSS` 中的整屏高度配置
- [x] 将 `NPC` 面板高度改为 `70vh`
- [x] 同步设置 `max-height: 70vh`
- [x] 将弹层交叉轴对齐改为居中显示
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台确认视觉效果

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
校验结果为 `JSON_OK True`，并确认关键样式已落地：`NPC_HEIGHT_70 True`、`NPC_MAX_HEIGHT_70 True`、`NPC_ALIGN_CENTER True`。

---

### 16:42 - 水上乐园弹层互斥修复

**内容摘要**：
- 根据用户进一步反馈，将 `NPC` 面板高度继续下调到 `60vh`。
- 重新排查遮挡问题后，确认更核心的原因是地图、特化、NPC 三个弹层可同时保持打开，导致右侧区域互相覆盖。

**完成任务**：
- [x] 将 `NPC` 面板高度由 `70vh` 调整为 `60vh`
- [x] 同步设置 `max-height: 60vh`
- [x] 为 `地图` 入口添加“打开前关闭特化/NPC”逻辑
- [x] 为 `特化` 入口添加“打开前关闭地图/NPC”逻辑
- [x] 为 `NPC` 入口添加“打开前关闭地图/特化”逻辑
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台确认遮挡是否彻底消失

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
本次命令行校验结果为 `JSON_OK True`，并确认关键修复已写入：`MAP_CLOSE_SPEC True`、`MAP_CLOSE_NPC True`、`SPEC_CLOSE_MAP True`、`SPEC_CLOSE_NPC True`、`NPC_CLOSE_MAP True`、`NPC_CLOSE_SPEC True`、`NPC_HEIGHT_60 True`、`NPC_MAX_HEIGHT_60 True`。

---

### 16:59 - 地图与特化改为右侧固定展开

**内容摘要**：
- 根据用户最新反馈，确认 `NPC` 二级菜单稳定而 `地图/特化` 仍被遮挡，根因是三者结构不一致。
- 将 `地图` 与 `特化` 从原来的全屏 `modal + 居中弹窗` 改为与 `NPC` 同类的右侧 `fixed` 展开面板，并同步更新互斥关闭逻辑。

**完成任务**：
- [x] 对照当前 JSON 与标准模板思路确认结构差异
- [x] 将地图模块改为右侧固定展开面板 `as-map-panel`
- [x] 将特化模块改为右侧固定展开面板 `as-spec-panel`
- [x] 移除地图与特化对旧 `as-map-modal/as-spec-modal` 结构的依赖
- [x] 同步更新 `NPC` 入口的互斥关闭目标
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台验证地图栏与特化库是否彻底不再被遮挡

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
命令行校验结果为 `JSON_OK True`；关键断言全部为真：`MAP_PANEL_NEW True`、`MAP_MODAL_OLD False`、`SPEC_PANEL_NEW True`、`SPEC_MODAL_OLD False`、`NPC_CLOSES_MAP_PANEL True`、`NPC_CLOSES_SPEC_PANEL True`。

---

### 17:06 - 回归修复 NPC 弹层失效

**内容摘要**：
- 用户反馈新一轮改动后出现“NPC 弹不出来、地图/特化仍遮挡”。
- 复盘后确认关键问题为样式链断裂：`地图/特化` 结构调整时，`NPC` 依赖的 `as-modal` 基础样式被丢失，导致 `NPC` 弹层定位与显示行为异常。

**完成任务**：
- [x] 定位 `NPC` 弹层依赖 `as-modal` 基础样式
- [x] 在 `NPC_CSS` 中补回 `as-modal` 基础样式定义
- [x] 保持 `NPC` 与地图/特化的互斥关闭逻辑指向新面板 ID
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台复测 NPC 弹层与地图/特化遮挡情况

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
校验结果：`JSON_OK True`，并确认 `HAS_AS_MODAL_BASE True`、`NPC_TRIGGER_USES_MODAL True`、`NPC_CLOSES_NEW_PANELS True`、`MAP_OLD_MODAL_REF False`、`SPEC_OLD_MODAL_REF False`。

---

### 17:16 - 纠正右侧面板层级误配

**内容摘要**：
- 用户指出 NPC 也被遮挡，复查后确认是层级策略误配：`地图/特化` 面板和入口与 `NPC` 处于同层或更高层，导致 `NPC` 触发器与弹层被压住。
- 采用最小修复方案：统一下调 `地图/特化` 层级，提升 `NPC` 层级到最高。

**完成任务**：
- [x] 将地图入口与地图面板层级调整为 `2147483645`
- [x] 将特化入口与特化面板层级调整为 `2147483645`
- [x] 将 NPC 入口与 NPC 弹层层级调整为 `2147483647`
- [x] 保持当前互斥关闭逻辑不变
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台确认 NPC 不再被遮挡，且地图/特化可正常弹出

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
校验结果为 `JSON_OK True`，关键样式断言：`as-map-wrap z-index=2147483645`、`as-map-panel z-index=2147483645`、`as-spec-wrap z-index=2147483645`、`as-spec-panel z-index=2147483645`、`as-npc-wrap z-index=2147483647`、`as-npc-modal z-index=2147483647`。

---

### 17:24 - 弹层遮挡稳定性修复（挂载到 body）

**内容摘要**：
- 用户明确目标是“解决弹窗被遮挡”，因此采用稳态修复：各弹层打开时先挂载到 `document.body`，规避父级层叠上下文和裁剪影响。
- 同时把地图/特化入口与面板层级恢复到最高层，避免在宿主环境中被外部节点覆盖。

**完成任务**：
- [x] 地图面板打开逻辑加入 `appendChild(document.body)` 挂载
- [x] 特化面板打开逻辑加入 `appendChild(document.body)` 挂载
- [x] NPC 弹层打开逻辑加入 `appendChild(document.body)` 挂载
- [x] 地图入口/面板层级统一提升到 `2147483647`
- [x] 特化入口/面板层级统一提升到 `2147483647`
- [x] 进行 JSON 有效性校验
- [x] 进行 IDE 诊断检查

**进行中任务**：
- [ ] 等待用户在宿主平台验证遮挡是否消失

**代码变更**：
- `MMD角色卡/RPG类/非线性剧情/水上乐园/sexytop_regex_碧波盛夏_V2_状态栏+NPC+地图+特化.json`

**备注**：
校验结果：`JSON_OK True`，关键断言为 `MAP_APPEND_BODY True`、`SPEC_APPEND_BODY True`、`NPC_APPEND_BODY True`、`MAP_WRAP_TOP_Z True`、`MAP_PANEL_TOP_Z True`、`SPEC_WRAP_TOP_Z True`、`SPEC_PANEL_TOP_Z True`。

---

### 17:46 - 固化二开规范文档（弹窗防遮挡最终方案）

**内容摘要**：
- 用户确认“弹窗遮挡问题已解决”，要求把最终方案沉淀到标准模板说明与二开指南，作为后续二开的强制规范。
- 已将“body 挂载 + fixed + 统一最高层级 + 互斥打开 + 禁止单点 z-index 修补”写入两份文档。

**完成任务**：
- [x] 更新标准模板说明文档的更新时间与层级策略
- [x] 在标准模板说明中新增“最终防遮挡方案（强制）”
- [x] 更新地图/特化/NPC 交互摘要为互斥打开 + body 挂载
- [x] 更新二开指南，新增“3.6 弹窗防遮挡最终方案（强制执行）”
- [x] 更新二开指南排错手册与案例记录，增加遮挡专项结论
- [x] 对改动文档执行 IDE 诊断检查（无报错）

**进行中任务**：
- [ ] 等待用户后续是否要求同步到更多模板文档/分支

**代码变更**：
- `资料库/正则资料/正则模板/标准模板_状态栏+侧边栏_V1_地图栏位与特化栏位说明.md`
- `资料库/正则资料/正则模板/标准模板_状态栏+侧边栏_V1_二次开发指南.md`

**备注**：
本次文档更新后，后续二开可直接按“强制方案”执行，避免再次出现“导入后弹层被遮挡/被裁切/互相误挡”的反复问题。

---

### 17:49 - 文档开头新增提测核对清单

**内容摘要**：
- 用户确认需要增加“提测前 10 秒核对清单”。
- 已将防遮挡强制检查项前置到两份规范文档顶部，便于二开提测时快速核对。

**完成任务**：
- [x] 在二次开发指南开头新增“提测前 10 秒核对清单（防遮挡强制版）”
- [x] 在标准模板说明开头新增同版核对清单
- [x] 覆盖四件套检查点：`body 挂载 + fixed + 同层级 + 互斥打开`
- [x] 补充“禁止只调单点 z-index”红线
- [x] 对改动文档执行 IDE 诊断检查（无报错）

**进行中任务**：
- [ ] 等待用户确认是否还要把清单同步到更多模板文档

**代码变更**：
- `资料库/正则资料/正则模板/标准模板_状态栏+侧边栏_V1_二次开发指南.md`
- `资料库/正则资料/正则模板/标准模板_状态栏+侧边栏_V1_地图栏位与特化栏位说明.md`

**备注**：
后续二开可先看文档开头清单再改代码，先过清单再提测，可显著降低遮挡回归概率。

---
