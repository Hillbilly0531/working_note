# 工作日志 - 2026-04-24

## 对话记录

### 14:21 - 统一数据库与 ES 地址配置

**内容摘要**：
- 按用户要求将数据库地址和 ES 地址统一收敛到 `bootstrap.yml`。
- 清理 CDC 任务中的 ES 硬编码，改为从 Spring 配置读取。

**完成任务**：
- [x] 在 `src/main/resources/bootstrap.yml` 新增各环境 `infra.mysql-host` 与 `infra.es-host`。
- [x] 将 `spring.datasource.dynamic.datasource.*.url` 中的主机改为引用 `${infra.mysql-host}`。
- [x] 在 `bootstrap.yml` 各环境新增 `spring.data.elasticsearch.{hosts,username,password}`。
- [x] 修改 `src/main/resources/cdc/jobs/pc-cash-cow-flow.yml`，移除 `sink.hosts/username/password` 硬编码。
- [x] 修改 `src/main/resources/cdc/jobs/saleout-full.yml`，移除 `sink.hosts/username/password` 硬编码。

**进行中任务**：
- [ ] 等待用户确认 prod/test 环境 ES 与 MySQL 地址默认值是否需要调整。

**代码变更**：
- `src/main/resources/bootstrap.yml`
- `src/main/resources/cdc/jobs/pc-cash-cow-flow.yml`
- `src/main/resources/cdc/jobs/saleout-full.yml`

**备注**：
已通过 IDE Lint 检查，新增告警均为 Spring 配置元数据无法识别自定义键或部分扩展键的提示，不影响 YAML 语法与运行时取值。

---

### 15:22 - 梳理项目服务器部署方案

**内容摘要**：
- 用户询问当前 `flinkcdc-es-client` 项目如何部署到服务器。
- 已结合 `pom.xml`、`Dockerfile`、`application.yml`、`bootstrap.yml` 梳理运行方式、依赖项与上线注意点。

**完成任务**：
- [x] 确认项目为 Maven + Spring Boot 可执行 Jar 部署形态。
- [x] 确认默认业务端口为 `24050`，管理端口为 `24051`。
- [x] 确认运行依赖包含 `Nacos`、`MySQL`、`Elasticsearch`，且 `prod` 环境默认通过 `c.nacos.leyo:80` 获取配置。
- [x] 确认仓库存在 `Dockerfile`，可支持容器化部署。
- [x] 整理裸机部署与 Docker 部署两套建议步骤，并提示 `prod` 启动时需显式指定 profile。

**进行中任务**：
- [ ] 待用户确认目标服务器环境（Linux 裸机 / Docker / k8s）后，可进一步给出对应启动脚本或部署清单。

**代码变更**：
- 无代码变更（本轮仅分析部署方案）。

**备注**：
当前 `Dockerfile` 的 `java -jar $JAVA_ARGS ... /app.jar` 参数顺序对 JVM 启动参数不友好；若容器启动时需要传 `-Xms/-Xmx`，建议先调整为 `java $JAVA_ARGS -jar /app.jar $APP_OPTS` 再上线。

### 14:56 - 统一项目命名为 flinkcdc-es-client

**内容摘要**：
- 用户要求将原 `solar-openapi` 相关命名统一改为 `flinkcdc-es-client`。
- 已同步修改应用名、日志名、构建产物名及 Log4j2 默认日志名。

**完成任务**：
- [x] 修改 `src/main/resources/bootstrap.yml`：`spring.application.name` -> `flinkcdc-es-client`。
- [x] 修改 `src/main/resources/application.yml`：`logging.file.name` -> `flinkcdc-es-client`。
- [x] 修改 `pom.xml`：`artifactId` -> `flinkcdc-es-client`（`finalName` 跟随 `artifactId`）。
- [x] 修改 `src/main/resources/log4j2-spring.xml`：`LOG_NAME` 默认值 -> `flinkcdc-es-client`。
- [x] 检查变更文件状态并执行 IDE Lint 读取（未引入新增错误，现有告警多为历史配置元数据提示）。

**进行中任务**：
- [ ] 待用户重启应用并确认 Nacos dataId/服务注册名切换是否符合预期。

**代码变更**：
- `src/main/resources/bootstrap.yml`
- `src/main/resources/application.yml`
- `pom.xml`
- `src/main/resources/log4j2-spring.xml`

**备注**：
更名后，依赖 `spring.application.name` 的 Nacos 默认 dataId（如 `${name}.yml`）与服务注册名会随之变化，需联动确认配置中心是否存在对应条目。

---

### 14:53 - 解释命名为何为 solar-openapi

**内容摘要**：
- 用户询问“为什么生成的名字是 solar-openapi”。
- 已核对应用名、日志名、构建产物名三个来源配置。

**完成任务**：
- [x] 确认 `bootstrap.yml` 中 `spring.application.name: solar-openapi`。
- [x] 确认 `application.yml` 中 `logging.file.name: solar-openapi`。
- [x] 确认 `pom.xml` 中 `artifactId: solar-openapi` 且 `finalName=${project.artifactId}`。

**进行中任务**：
- [ ] 待用户决定是否统一改名以及需要影响的范围（日志名/应用名/jar 名）。

**代码变更**：
- 无代码变更（本轮仅说明配置来源）。

**备注**：
命名为 `solar-openapi` 属于多处显式配置，不是运行时随机生成。

---

### 14:51 - 日志输出编码显式改为 UTF-8

**内容摘要**：
- 用户确认执行日志编码修复。
- 已在 Log4j2 配置中为控制台与滚动文件输出显式指定 UTF-8。

**完成任务**：
- [x] 修改 `src/main/resources/log4j2-spring.xml` 的 `Console` appender `PatternLayout`，增加 `charset="UTF-8"`。
- [x] 修改 `src/main/resources/log4j2-spring.xml` 的 `RollingFile` appender `PatternLayout`，增加 `charset="UTF-8"`。
- [x] 对改动文件执行 IDE Lint 检查，无报错。

**进行中任务**：
- [ ] 等待用户重启应用后确认新日志文件中文显示是否正常。

**代码变更**：
- `src/main/resources/log4j2-spring.xml`

**备注**：
该修复仅影响本应用日志写出编码；Nacos 共享配置本身若非 UTF-8，仍需在 Nacos 侧修复。

---

### 14:50 - 检查“写入文档是否 UTF-8”代码路径

**内容摘要**：
- 用户要求确认是否是写入时未使用 UTF-8 导致乱码。
- 已检查项目内 Nacos 配置读写相关代码与日志落盘配置。

**完成任务**：
- [x] 全仓搜索 `publishConfig/ConfigService`，未发现本项目主动写入 Nacos 配置的代码。
- [x] 检查 `bootstrap.yml`，确认当前项目仅“读取”共享配置 `rabbitmq.yaml`、`mail.yaml`。
- [x] 检查资源读取实现 `ResourceContentLoader`，使用 `StandardCharsets.UTF_8`。
- [x] 检查 `log4j2-spring.xml`，发现 `PatternLayout` 未显式声明 charset，默认跟随 JVM 平台编码。

**进行中任务**：
- [ ] 待用户决定是否要将 `log4j2-spring.xml` 的 `PatternLayout` 显式加 `charset="UTF-8"`，统一日志输出编码。

**代码变更**：
- 无代码变更（本轮仅排查）。

**备注**：
当前错误栈里的 `MalformedInputException` 更符合 Nacos 侧配置内容编码异常，而非本项目写入 Nacos 导致。

---

### 14:48 - 日志乱码与 Nacos 配置解析失败排查

**内容摘要**：
- 用户反馈 `solar-openapi.log` 中出现乱码。
- 读取日志后确认不仅是显示乱码，还存在 `MalformedInputException`，导致 Nacos YAML 解析失败。

**完成任务**：
- [x] 读取 `logs/solar-openapi.log` 头部错误栈并定位异常类型。
- [x] 结合 `bootstrap.yml` 确认出错数据源来自共享配置 `rabbitmq.yaml`、`mail.yaml`（Nacos）。
- [x] 明确根因方向：Nacos 配置内容编码不是 UTF-8（或含非法字节），被 SnakeYAML 读取时解码失败。

**进行中任务**：
- [ ] 待用户在 Nacos 侧将相关 dataId 内容转存为 UTF-8（无 BOM）后复测启动。

**代码变更**：
- 无代码变更（本轮仅排查）。

**备注**：
该问题会影响配置加载，属于“编码+解析失败”，不是单纯日志显示问题。

---

### 14:46 - 查询项目运行日志目录

**内容摘要**：
- 用户询问本项目运行日志存放位置。
- 根据应用配置确认日志目录与日志文件名。

**完成任务**：
- [x] 读取 `src/main/resources/application.yml` 中 `logging.file.path` 与 `logging.file.name`。
- [x] 返回日志目录与实际日志文件路径。

**进行中任务**：
- [ ] 无。

**代码变更**：
- 无代码变更（仅查询配置）。

**备注**：
当前配置为相对路径 `logs`，即项目根目录下日志文件。

---

### 14:37 - 排查 wmsSplitItemFull 抽数卡住原因

**内容摘要**：
- 用户反馈 `wmsSplitItemFull` 线程栈大量 WAITING/TIMED_WAITING，怀疑任务抽不到数据。
- 结合终端日志确认任务处于 Flink CDC `INITIAL` 快照阶段，`debezium-reader-0` 正在 MySQL Socket 读取；随后应用被关闭触发 `InterruptedException`。

**完成任务**：
- [x] 读取并分析终端日志中的线程栈与异常顺序。
- [x] 定位 `CdcJobRunner` 启动诊断逻辑，确认 `cdc-startup-diagnostic` 会周期性打印线程栈，不等同于失败。
- [x] 确认启动失败直接原因是 `SpringContextShutdownHook` 后 `environment.execute()` 被中断。

**进行中任务**：
- [ ] 等待用户确认是否手动停止进程/IDE 热重启，并补充应用关闭前首条 ERROR 以进一步确认触发源。

**代码变更**：
- 无代码变更（本轮仅日志排查）。

**备注**：
当前日志证据更符合“快照执行中被中断”，而非 SQL 语法或 ES 配置导致的即时失败。

---

### 14:59 - 排查启动后未监听到数据

**内容摘要**：
- 用户要求检查 `logs/flinkcdc-es-client.log`，确认为什么应用启动后没有监听到数据。
- 已结合启动日志、任务 YAML 与 CDC 路由代码确认：任务成功启动，但卡在 `INITIAL` 全量快照阶段，尚未进入增量 binlog 监听。

**完成任务**：
- [x] 读取并分析 `logs/flinkcdc-es-client.log` 中 `wmsSplitItemFull` 启动链路。
- [x] 确认 MySQL 源、表发现、Flink Job 与 ES sink 初始化均已成功。
- [x] 确认目标表 `db_warehouse.pda_req_pick_split_item` 预估约 `61945079` 行，被切成 `7652` 个 snapshot chunk。
- [x] 确认 `debezium-reader-0` 持续阻塞在首个 snapshot 查询结果读取，日志中尚无“收到事件/路由主键/flush ES”记录。
- [x] 核对 `PrimaryKeyRoutingFlatMapFunction` 与 `CdcEventSelector`，确认 `INITIAL + INSERT` 不会过滤 snapshot 事件。

**进行中任务**：
- [ ] 待用户决定是继续等待全量快照跑完，还是改为 `LATEST_OFFSET` 仅监听启动后的增量。

**代码变更**：
- 无代码变更（本轮仅日志排查）。

**备注**：
当前“没监听到数据”的直接原因不是 CDC 任务没启动，也不是事件被 `listenOps` 过滤，而是 `INITIAL` 模式先做大表全量快照，完成前不会切到增量监听。

---

### 15:05 - 确认 CDC 写入索引

**内容摘要**：
- 用户询问当前 `wmsSplitItemFull` 任务实际写入的是哪个 Elasticsearch 索引。
- 已核对任务 YAML 配置与运行日志中的模板同步信息。

**完成任务**：
- [x] 读取 `src/main/resources/cdc/jobs/wms-splititem-full.yml` 的 `sink.index` 配置。
- [x] 检查 `logs/flinkcdc-es-client.log`，确认运行时模板名与配置一致。

**进行中任务**：
- [ ] 如用户需要，可继续核对是否已真正产生 bulk 写入记录。

**代码变更**：
- 无代码变更（本轮仅查询配置）。

**备注**：
当前任务配置的目标索引为 `wms-splititem-20260424`，模板名同样为 `wms-splititem-20260424`。

---

### 15:08 - 排查索引不存在的 404

**内容摘要**：
- 用户反馈对 `wms-splititem-20260424` 执行 `_search` 返回 `index_not_found_exception`。
- 已结合本地 CDC 日志、Sink 实现与 Elasticsearch 文档说明确认：模板同步不会直接创建索引，当前也尚未发生任何实际写入。

**完成任务**：
- [x] 检索 `logs/flinkcdc-es-client.log` 中是否出现 `flush`、`bulk`、`ES 请求失败`、`收到事件` 等日志。
- [x] 确认当前日志仅有 sink 初始化与模板同步记录，没有任何一条真正的 ES 写入记录。
- [x] 通过 Elasticsearch 文档核对：index template 只会在索引创建时应用，不会因保存模板而立即创建索引。

**进行中任务**：
- [ ] 如用户需要，可继续判断是“全量快照未结束”还是“首批写入后被 SQL 回查为空”。

**代码变更**：
- 无代码变更（本轮仅排查）。

**备注**：
当前最直接的原因是 `wms-splititem-20260424` 还没被显式创建，也还没有首笔成功写入触发自动建索引，因此 `_search` 直接报 404 属于 Elasticsearch 正常行为。

---

### 15:10 - 手工创建 ES 索引

**内容摘要**：
- 用户要求手工创建 `wms-splititem-20260424`。
- 已在目标 ES 上执行建索引并用 `_search` 再次验证，不再返回 404。

**完成任务**：
- [x] 查询模板 `/_template/wms-splititem-20260424`，确认模板已存在。
- [x] 执行 `PUT /wms-splititem-20260424` 手工创建索引。
- [x] 执行 `GET /wms-splititem-20260424`，确认索引创建成功。
- [x] 执行 `POST /wms-splititem-20260424/_search`（`size=0`）验证查询可用，当前文档数为 0。

**进行中任务**：
- [ ] 如用户需要，可继续排查为什么 CDC 仍未向该索引写入首批文档。

**代码变更**：
- 无代码变更（本轮为外部 ES 操作）。

**备注**：
验证结果显示索引已存在且可查询，但当前 `hits.total.value = 0`，说明手工建索引只解决了 404，不代表 CDC 已开始写数。

---

### 15:14 - 继续排查为何仍未写入

**内容摘要**：
- 用户反馈即使索引已手工创建，CDC 仍然没有写入文档。
- 已继续核对最新日志与任务配置，发现当前既存在“首个 snapshot 仍在执行”的现象，也存在 `primaryKeyField` 与源表实际主键字段名不一致的高风险配置。

**完成任务**：
- [x] 读取最新 `flinkcdc-es-client.log` 尾部日志，确认 `debezium-reader-0` 仍在持续导出首个 split 数据。
- [x] 确认日志已出现 `Exported 305289 records for split ...`，说明快照在推进，不是作业停死。
- [x] 复核 `wms-splititem-full.yml` 与 `fullsplititem.sql`，发现路由主键配置为 `id`，但 SQL 实际用 `si.req_pick_split_item_id` 作为源表主键并在查询结果中别名为 `id`。
- [x] 复核 Java 代码，确认 `lookup.primaryKeyField` 同时用于 CDC 事件主键提取与 SQL 回查入参，不区分“源表字段名”和“结果别名”。

**进行中任务**：
- [ ] 待用户确认是否直接修正 `wms-splititem-full.yml` 的 `primaryKeyField` 为源表 CDC 字段名并重启验证。

**代码变更**：
- 无代码变更（本轮仅排查）。

**备注**：
当前最可疑的根因是 `primaryKeyField: id` 会导致从 CDC 事件里取不到主键；即使快照结束进入事件处理，后续也大概率无法正确路由到 SQL 回查与 ES 写入。

---

### 15:19 - 确认 chunk size 修改位置

**内容摘要**：
- 用户询问 Flink CDC 的 chunk size 在哪里修改。
- 已核对当前项目的 `CdcJobRunner`、任务 YAML 与 Flink CDC 文档，确认项目里尚未把该参数暴露到配置文件。

**完成任务**：
- [x] 检查 `src/main/java/com/leyo/cdc/job/CdcJobRunner.java`，确认当前仅设置了 hostname/port/tableList/serverId/startupOptions 等基础参数。
- [x] 检查 `src/main/resources/cdc/jobs/wms-splititem-full.yml`，确认当前没有 `chunk size` 相关字段。
- [x] 结合 Flink CDC 文档确认：首轮快照 chunk 参数对应 `scan.incremental.snapshot.chunk.size`，在 Java builder 模式下需要在 `MySqlSourceBuilder` 这一层补充设置。

**进行中任务**：
- [ ] 如用户需要，可继续直接把 chunk size、fetch size、并行快照相关配置接入当前项目。

**代码变更**：
- 无代码变更（本轮仅定位修改点）。

**备注**：
当前仓库里改 chunk size 的正确入口不是现有 YAML，而是 `CdcJobRunner` 中 `MySqlSource.builder()` 后的参数设置代码；若要做成可配置，还需要同步扩展 `CdcJobDefinition`。

---

### 14:31 - 关闭 saleout 并接入 wms-splititem 迁移任务

**内容摘要**：
- 按用户要求先关闭 `saleout` 任务，再新增 `wms-splititem` 全量迁移任务。
- 将 `D:\IdeaProjects\datacenter_logstash\config\wms-splititem\sql\fullsplititem.sql` 迁移到当前项目并接入 CDC Job。

**完成任务**：
- [x] 修改 `src/main/resources/cdc/jobs/saleout-full.yml`，将 `enabled` 调整为 `false`。
- [x] 新增 `src/main/resources/cdc/sql/fullsplititem.sql`。
- [x] 新增 `src/main/resources/cdc/es/wms_splititem_mapping.json`。
- [x] 新增 `src/main/resources/cdc/jobs/wms-splititem-full.yml` 并设置 `enabled: true`。
- [x] 执行改动后校验：目标文件无 IDE Lint 报错。

**进行中任务**：
- [ ] 本机缺少 Maven 命令（`mvn` 不可用），待用户环境补齐后执行打包/启动完成实际迁移运行验证。

**代码变更**：
- `src/main/resources/cdc/jobs/saleout-full.yml`
- `src/main/resources/cdc/jobs/wms-splititem-full.yml`
- `src/main/resources/cdc/sql/fullsplititem.sql`
- `src/main/resources/cdc/es/wms_splititem_mapping.json`

**备注**：
本次仅调整任务编排与资源文件；迁移运行需在具备 Maven 与目标环境连接信息的机器上启动应用后生效。

---
